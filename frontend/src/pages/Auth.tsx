import LoginForm from "@/auth/login/loginForm";

function Auth() {
  return (
    <section className="relative min-h-screen px-5 md:px-8 py-2 max-w-md sm:max-w-xl md:max-w-7xl mx-auto">
      <LoginForm />
    </section>
  );
}

export default Auth;
